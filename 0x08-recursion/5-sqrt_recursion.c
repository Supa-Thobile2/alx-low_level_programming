#include "main.h"

/**
 *_sqrt_recursion - finds the square root of input number
 *@num: number to fingd square root of
 *@root: the root to be tested
 *
 * Return: 
 */

int _sqrt_recursion(int n)
{
	if ((root * root) == number)
		return (root);
}
